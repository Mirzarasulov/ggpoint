import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export const metadata = { title: "Каталог" };

export default function CatalogPage() {
  return (
    <PlaceholderPage
      title="Каталог"
      description="Скоро здесь появятся все товары: фильтры, сортировка, карточки с ценами. Подключаем базу на следующей фазе."
    />
  );
}
